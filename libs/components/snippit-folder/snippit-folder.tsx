import {FolderProps} from "utils";

export const SnippitFolderComponent = ({folderName}: FolderProps) => {
    return (
        <>
            <button>
                <img/>
            </button>
            <p>{}</p>
        </>
    )
}